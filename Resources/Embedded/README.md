Embedded resources used by the plugin live here.

Files named `NXO.Resources.*` are embedded by `NXOSource.csproj` with their filename as the logical resource name. Keep those filenames stable unless every matching `AssetHandler` lookup is updated too.
